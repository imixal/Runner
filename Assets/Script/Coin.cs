using System.Collections;
using System.Collections.Generic;
using DG.Tweening;
using UnityEngine;

public class Coin : MonoBehaviour
{
    
    /*  private int direction = 1;
    void Start()
    {

        DoRotate();
    }

    private void DoRotate()
    {
        var tweener = transform.DORotate(new Vector3(0, 180 * direction, 0), 1, RotateMode.FastBeyond360);
        direction++;
        tweener.onComplete += DoRotate;
    }*/
}

